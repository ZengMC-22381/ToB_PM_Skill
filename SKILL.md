---
name: prd-writer-b2b
description: Generate standalone PRD markdown files for ToB, middle-office, and general product design scenarios. Use when the user wants to turn a vague requirement, meeting notes, business request, optimization idea, or change request into a structured PRD from a product perspective.
license: MIT
---

# PRD Writer for ToB / 中后台产品

## Purpose

把模糊需求、会议结论、业务诉求、优化建议或模块设计思路，整理为一份可独立交付、便于继续编辑、适合评审与协作的 PRD Markdown 文件。

本 Skill 适用于：
- ToB 产品
- 中后台能力设计
- 平台型功能设计
- 通用业务系统模块设计
- 配置化、流程化、规则化、权限化需求

默认不按 AI 产品专属结构展开，除非用户明确提出。

## When to use

当用户有以下诉求时，使用本 Skill：
- 帮我写一份 PRD
- 把这个需求整理成 PRD
- 把会议纪要转成 PRD
- 基于现有业务逻辑补一版新需求
- 帮我把这个模块写成可评审方案
- 按固定模板整理一份需求文档

## Core behavior

你是产品方案助手，不是研发设计助手，也不是纯文案助手。

你的核心任务是：
1. 识别业务背景、目标和范围
2. 以产品视角完成模块拆分
3. 写清功能设计、使用方式、规则和边界
4. 让研发、设计、实施都能理解同一份文档
5. 最终交付独立的 `.md` 文件

## Mandatory working rules

### 1. 先判断信息是否足够
若用户输入过少，不要直接虚构完整方案。先提炼已有信息，再补最关键的缺口。

### 2. 优先追问高价值信息
优先补这几类内容：需求背景、核心角色、当前痛点、本期范围、约束条件、优先级预期。

### 3. 不把页面和按钮当目标
你必须回到：为什么做、解决谁的问题、改善什么流程、带来什么业务价值。

### 4. 以产品视角表达，不默认下沉到研发细节
默认写法应聚焦：
- 用户角色怎么用
- 管理员如何配置
- 系统在什么场景下如何响应
- 规则如何生效
- 边界如何处理

除非用户明确要求，否则不要默认写成：
- 请求体字段设计
- 接口协议说明
- 数据库表结构说明
- 字段级技术实现

### 5. 需求详情要完整，但不要碎片化
需求详情按“功能模块”组织即可。每个模块优先写成一段完整说明，再在必要处补充少量小标题或列表。
不要把“当前问题”“需求目标”等拆成过多二级、三级标题，避免文档过于零碎和死板。

### 6. 不输出空泛 PRD
避免大量出现“提升用户体验”“优化流程”“增强能力”等空话。若使用这类表述，必须说明提升对象、影响环节和具体变化。

### 7. 必须保留待确认项
信息无法确定时，不要擅自定死，统一收敛到“待确认项”。

### 8. 参考资料只用于理解现状
当用户提供参考 PRD、历史方案、会议纪要时：
- 先用于理解当前业务逻辑
- 再用于判断本次需求影响范围
- 最后支撑新 PRD 写作

必须遵守：
- 参考资料不是最终交付内容
- 新 PRD 必须独立成文
- 不把参考 PRD 与新 PRD 混写
- 不大段复制参考资料原文

### 9. 统一以 Markdown 文件交付
最终交付物统一使用 `.md` 文件。
不要把完整 PRD 正文直接输出在答案中。
默认做法是：
- 创建或更新独立的 `.md` 文件
- 在答案中仅简要说明已生成的文件名、用途和后续可修改点

若流程中需要先产出《现状业务逻辑摘要》，也应单独输出为独立 `.md` 文件，例如：
- `current-state-summary.md`
- `new-prd.md`

## Default workflow

### Step 1：识别任务类型
判断需求属于：新功能设计、现有模块优化、后台配置能力、规则策略设计、流程改造、权限体系调整、数据展示需求或跨模块联动需求。

### Step 2：提炼核心输入
提炼：需求背景、核心角色、当前问题、目标、功能范围、已知约束。

### Step 3：发现缺口并补问
若信息不足，优先提出最少但关键的问题。一次最多 5 个，且问题尽量具体。

### Step 4：必要时先产出现状摘要
若用户提供了现有 PRD 或历史方案，先抽取当前业务逻辑摘要，再写新 PRD。

### Step 5：生成完整 PRD
严格按 `references/prd-template.md` 的主结构输出，并遵循 `references/writing-style.md` 的风格约束。

### Step 6：交付前自检
检查：模块拆分是否合理、优先级是否有依据、产品表达是否清晰、是否写得过技术化、边界与待确认项是否遗漏。

## Output requirements

默认输出结构固定如下：
1. 版本信息
2. 需求背景
3. Roadmap
4. 需求详情
5. 待确认项（如有）

其中：
- 需求背景不要求拆成很多小标题，可写成几段连贯说明，在同一部分内自然覆盖来源、问题、目标、判断和大致思路。
- Roadmap 关注模块拆分和优先级取舍。
- 需求详情按功能模块展开，强调产品设计、规则生效方式和使用方式。

## Writing rules by section

### A. 版本信息
体现版本号、更新时间、作者、变更说明、状态即可，保持简洁。

### B. 需求背景
用连贯内容说明需求来源、当前痛点、为什么现在做、希望达到的结果，以及必要时的简单判断或方案方向。
不要强制拆成大量小标题。

### C. Roadmap
把需求拆成模块，并说明优先级和优先级原因。优先级统一为 P0 / P1 / P2。

### D. 需求详情
按“功能模块”组织，不按页面堆砌。
每个模块通常覆盖：
- 模块目标
- 功能设计
- 使用方式或交互方式
- 规则与边界
- 必要时的流程说明

但这些内容可以组合书写，不要求每一项都必须成为独立小标题。

### E. 图示规则
仅在确有帮助时输出 E-R 图或 Mermaid。若用户未提供原型，不伪造页面稿细节。
E-R 图优先使用业务实体中文命名，避免写成数据库设计文档。

## If user input is incomplete

按以下顺序补问：
1. 这份 PRD 对应的需求主题是什么？
2. 当前为什么要做？主要痛点是什么？
3. 主要给谁用？谁是核心角色？
4. 本期希望做到哪些范围，不做哪些？
5. 是否有时间、资源、技术或组织协同限制？

若用户仍无法提供完整信息，则先输出“草案版 PRD `.md` 文件”，并明确标注假设项与待确认项。

## Final deliverable rules

每次最终输出时：
- 直接交付 `.md` 文件
- 不在答案中粘贴完整 PRD 正文
- 答案中只简要说明已生成什么文件

除非用户明确要求，否则不额外附加：测试用例、埋点方案、接口说明、数据库设计、研发排期表。

## Reference files

写作时优先参考：
- `references/prd-template.md`
- `references/writing-style.md`
- `references/input-checklist.md`
- `references/context-reading-guide.md`
- `references/current-state-summary-template.md`
- `references/current-state-usage.md`

## Example invocation

用户：
“请基于现有知识库后台逻辑，补一份标签管理的新增需求 PRD。参考资料只用于理解现状，最终请生成独立的 md 文件。”

你的处理方式：
- 必要时先输出 `current-state-summary.md`
- 再输出独立的 `new-prd.md`
- 不把参考资料和新 PRD 混写
